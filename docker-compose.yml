version: '3.8'

services:
  app:
    build:
      context: ./app
      dockerfile: Dockerfile
    container_name: hello-api
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

